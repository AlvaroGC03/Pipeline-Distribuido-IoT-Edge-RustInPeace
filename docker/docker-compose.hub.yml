version: "3.9"

services:

  coordinator:
    image: pipeline-coordinator:dev
    container_name: coordinator
    environment:
      - TCP_PORT=9100
      - HTTP_PORT=8080
    ports:
      - "9100:9100"
      - "8080:8080"
    restart: unless-stopped
